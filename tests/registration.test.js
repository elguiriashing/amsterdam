import {test,before,after} from 'node:test';
import assert from 'node:assert/strict';
import express from 'express';
import supertest from 'supertest';
import crypto from 'node:crypto';
import {MongoMemoryReplSet} from 'mongodb-memory-server';
import {MongoClient} from 'mongodb';
import {registrationRouter,setupRegistration} from '../registration.js';
import {adult,details,ocrSuggestions} from '../registration-domain.js';
let repl,client,db,api;
process.env.JWT_SECRET='test-only-secret';
const body=()=>({firstName:'Test',surname:'Member',dob:'1990-06-15',phone:'+34600000000',email:'test@example.com',address:{line:'Calle Test 1',city:'Fuengirola',postcode:'29640',country:'ES'},documentNumber:'TEST'+crypto.randomBytes(5).toString('hex'),ageConfirmed:true,privacyAccepted:true,submissionKey:crypto.randomUUID()});
before(async()=>{repl=await MongoMemoryReplSet.create({replSet:{count:1},binary:{version:'7.0.14'}});client=new MongoClient(repl.getUri());await client.connect();db=client.db('tests');await setupRegistration(db);const app=express();app.use(express.json());app.use('/',registrationRouter({getDB:()=>db,client,authenticateToken:(req,res,next)=>{if(!req.headers.authorization)return res.sendStatus(401);req.user={id:req.headers.authorization==='owner'?'admin':'member'};next();},isAdmin:req=>req.user.id==='admin',prefillLimiter:(req,res,next)=>next(),logAudit:async()=>{},notify:async()=>{}}));api=supertest(app);});
after(async()=>{await client?.close();await repl?.stop();});
test('age and address validation reject malformed, underage and non-Spanish data',()=>{assert.equal(adult('2008-09-19',new Date('2026-09-19T12:00:00Z')),true);assert.equal(adult('2008-09-20',new Date('2026-09-19T12:00:00Z')),false);assert.equal(adult('2000-02-31'),false);assert.throws(()=>details({...body(),address:{country:'FR'}}));});
test('member tokens cannot list registrations, reserve numbers or retrieve IDs',async()=>{for(const path of ['/prefills','/numbers','/prefills/aaaaaaaaaaaaaaaaaaaaaaaa/id-image'])assert.equal((await api.get(path).set('Authorization','member')).status,403);assert.equal((await api.post('/numbers/reserve').set('Authorization','member').send({})).status,403);});
test('duplicate submission retries save exactly one prefill and do not disclose new upload token',async()=>{const b=body();const rs=await Promise.all(Array.from({length:4},()=>api.post('/prefill').send(b)));assert.equal(await db.collection('prefills').countDocuments({submissionKey:b.submissionKey}),1);assert.equal(new Set(rs.map(r=>r.body.id)).size,1);assert.equal(rs.filter(r=>r.body.uploadToken).length,1);});
test('number setup considers legacy members and paper history; simultaneous reservations are unique',async()=>{await db.collection('members').insertOne({email:'400',name:'Legacy'});assert.equal((await api.post('/numbers/setup').set('Authorization','owner').send({next:300,paperChecked:true})).status,400);assert.equal((await api.post('/numbers/setup').set('Authorization','owner').send({next:500,paperChecked:true})).status,200);const rs=await Promise.all(Array.from({length:8},()=>api.post('/numbers/reserve').set('Authorization','owner').send({requestId:crypto.randomUUID()})));assert.ok(rs.every(r=>r.status===200),JSON.stringify(rs.map(r=>r.body)));assert.equal(new Set(rs.map(r=>r.body.number)).size,8);const key=crypto.randomUUID();const a=await api.post('/numbers/reserve').set('Authorization','owner').send({requestId:key});const b=await api.post('/numbers/reserve').set('Authorization','owner').send({requestId:key});assert.equal(a.body.number,b.body.number);});
test('prepare and activate are idempotent; paper reservation can be claimed once',async()=>{const paper=await api.post('/numbers/reserve').set('Authorization','owner').send({requestId:crypto.randomUUID()});const r=await api.post('/prefill').send(body());const id=r.body.id;assert.equal((await api.post(`/prefills/${id}/prepare`).set('Authorization','owner').send({})).status,400);const payload={identityChecked:true,addressChecked:true,paperNumber:paper.body.number};const rs=await Promise.all([1,2].map(()=>api.post(`/prefills/${id}/prepare`).set('Authorization','owner').send(payload)));assert.ok(rs.every(r=>r.status===200));assert.equal(rs[0].body.memberNumber,paper.body.number);assert.equal(rs[1].body.memberNumber,paper.body.number);assert.equal((await api.post(`/prefills/${id}/activate`).set('Authorization','owner').send({signed:false})).status,400);const activations=await Promise.all([1,2].map(()=>api.post(`/prefills/${id}/activate`).set('Authorization','owner').send({signed:true})));assert.ok(activations.every(r=>r.status===200));assert.equal(await db.collection('members').countDocuments({registrationId:id}),1);assert.equal(activations.filter(r=>r.body.password).length,1);assert.equal((await api.get(`/prefills/${id}/form.pdf`).set('Authorization','owner')).status,409);});
test('edited prepared data must be reviewed again',async()=>{const b=body(),r=await api.post('/prefill').send(b),id=r.body.id;await api.post(`/prefills/${id}/prepare`).set('Authorization','owner').send({identityChecked:true,addressChecked:true});await api.patch(`/prefills/${id}`).set('Authorization','owner').send({...b,firstName:'Corrected'});assert.equal((await api.post(`/prefills/${id}/activate`).set('Authorization','owner').send({signed:true})).status,409);});
test('private fields are excluded and missing storage fails closed',async()=>{const r=await api.post('/prefill').send(body());const p=await api.get(`/prefills/${r.body.id}`).set('Authorization','owner');assert.equal(p.body.uploadTokenHash,undefined);assert.equal(p.body.submissionKey,undefined);const cfg=await api.get('/config');assert.equal(cfg.body.idUploads,false);assert.equal((await api.put(`/prefills/${r.body.id}/id-image`).set('Content-Type','image/jpeg').set('X-Upload-Token','wrong').send(Buffer.from('not an image'))).status,403);});
